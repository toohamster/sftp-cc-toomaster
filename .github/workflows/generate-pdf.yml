name: Generate PDF from Markdown

on:
  push:
    branches: [write-skill-book]
    paths:
      - 'myskillNotes/**/*.md'
      - '.github/workflows/generate-pdf.yml'

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pandoc \
            texlive-xetex \
            texlive-fonts-recommended \
            texlive-lang-chinese \
            texlive-lang-japanese \
            fonts-noto-cjk

      - name: Create output directory
        run: mkdir -p pdfs

      - name: Generate PDF for English chapters
        run: |
          echo "Generating English PDFs..."
          for file in myskillNotes/en/*.md; do
            filename=$(basename "$file" .md)
            echo "  -> $filename.pdf"
            pandoc "$file" \
              -o "pdfs/${filename}.pdf" \
              --pdf-engine=xelatex \
              -V documentclass=report \
              -V geometry:margin=1in \
              -V mainfont="Noto Serif CJK SC" \
              -V monofont="Noto Sans Mono CJK SC"
          done

      - name: Generate PDF for Japanese chapters
        run: |
          echo "Generating Japanese PDFs..."
          for file in myskillNotes/ja/*.md; do
            filename=$(basename "$file" .md)
            echo "  -> $filename.pdf"
            pandoc "$file" \
              -o "pdfs/${filename}.pdf" \
              --pdf-engine=xelatex \
              -V documentclass=report \
              -V geometry:margin=1in \
              -V mainfont="Noto Serif CJK JP" \
              -V monofont="Noto Sans Mono CJK JP"
          done

      - name: Generate PDF for Chinese chapters
        run: |
          echo "Generating Chinese PDFs..."
          for file in myskillNotes/zh-cn/*.md; do
            filename=$(basename "$file" .md)
            echo "  -> $filename.pdf"
            pandoc "$file" \
              -o "pdfs/${filename}.pdf" \
              --pdf-engine=xelatex \
              -V documentclass=report \
              -V geometry:margin=1in \
              -V mainfont="Noto Serif CJK SC" \
              -V monofont="Noto Sans Mono CJK SC"
          done

      - name: Combine all English chapters into one book
        run: |
          echo "Combining English book..."
          # Preprocess markdown files for PDF generation
          .github/pdf-helpers/pdf-preprocess.sh myskillNotes/en /tmp/pdf-preprocess/en
          # Combine files, remove internal --- but keep separator before chapters
          {
            cat /tmp/pdf-preprocess/en/README.md /tmp/pdf-preprocess/en/authors.md | sed '/^---$/d'
            echo ""
            echo "---"
            echo ""
            cat /tmp/pdf-preprocess/en/chapter-*.md
          } > pdfs/en-book-combined.md
          pandoc pdfs/en-book-combined.md \
            -o "pdfs/sftp-cc-book-en.pdf" \
            --pdf-engine=xelatex \
            -V documentclass=report \
            -V geometry:margin=1in \
            -V mainfont="Noto Serif CJK SC" \
            -V monofont="Noto Sans Mono CJK SC" \
            --metadata title="sftp-cc: Claude Code Skill Development Book" \
            --metadata author="toohamster"
          rm -rf /tmp/pdf-preprocess/en pdfs/en-book-combined.md

      - name: Combine all Chinese chapters into one book
        run: |
          echo "Combining Chinese book..."
          # Preprocess markdown files for PDF generation
          .github/pdf-helpers/pdf-preprocess.sh myskillNotes/zh-cn /tmp/pdf-preprocess/zh-cn
          # Combine files, remove internal --- but keep separator before chapters
          {
            cat /tmp/pdf-preprocess/zh-cn/README.md /tmp/pdf-preprocess/zh-cn/authors.md | sed '/^---$/d'
            echo ""
            echo "---"
            echo ""
            cat /tmp/pdf-preprocess/zh-cn/chapter-*.md
          } > pdfs/zh-cn-book-combined.md
          pandoc pdfs/zh-cn-book-combined.md \
            -o "pdfs/sftp-cc-book-zh-cn.pdf" \
            --pdf-engine=xelatex \
            -V documentclass=report \
            -V geometry:margin=1in \
            -V mainfont="Noto Serif CJK SC" \
            -V monofont="Noto Sans Mono CJK SC" \
            --metadata title="sftp-cc: Claude Code Skill 开发教程" \
            --metadata author="toohamster"
          rm -rf /tmp/pdf-preprocess/zh-cn pdfs/zh-cn-book-combined.md

      - name: Combine all Japanese chapters into one book
        run: |
          echo "Combining Japanese book..."
          # Preprocess markdown files for PDF generation
          .github/pdf-helpers/pdf-preprocess.sh myskillNotes/ja /tmp/pdf-preprocess/ja
          # Combine files, remove internal --- but keep separator before chapters
          {
            cat /tmp/pdf-preprocess/ja/README.md /tmp/pdf-preprocess/ja/authors.md | sed '/^---$/d'
            echo ""
            echo "---"
            echo ""
            cat /tmp/pdf-preprocess/ja/chapter-*.md
          } > pdfs/ja-book-combined.md
          pandoc pdfs/ja-book-combined.md \
            -o "pdfs/sftp-cc-book-ja.pdf" \
            --pdf-engine=xelatex \
            -V documentclass=report \
            -V geometry:margin=1in \
            -V mainfont="Noto Serif CJK JP" \
            -V monofont="Noto Sans Mono CJK JP" \
            --metadata title="sftp-cc: Claude Code Skill 開発ガイド" \
            --metadata author="toohamster"
          rm -rf /tmp/pdf-preprocess/ja pdfs/ja-book-combined.md

      - name: List generated PDFs
        run: |
          echo "Generated PDFs:"
          ls -lh pdfs/*.pdf

      - name: Upload PDFs as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: skill-book-pdfs
          path: pdfs/*.pdf
          retention-days: 30

      - name: Commit and push PDFs to branch
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add pdfs/
          git commit -m "docs: auto-generate PDFs from Markdown [skip ci]" || exit 0
          git push
